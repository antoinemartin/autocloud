common:
  repoURL: https://github.com/antoinemartin/autocloud.git
  targetRevision: main

spec:
  destination:
    server: https://kubernetes.default.svc
  source:
    {}
    # repoURL: "{{ .Values.common.repoUrl }}"
    # targetRevision: "{{ .Values.common.targetRevision }}"

traefik:
  chartVersion: "10.19.5"
  tlsOnly: true
  dashboard:
    expose: false
    insecure: false

uninode: false

apps:
  enabled: false
  source:
    path: apps/default
    # repoURL: "{{ .Values.common.repoUrl }}"
    # targetRevision: "{{ .Values.common.targetRevision }}"
